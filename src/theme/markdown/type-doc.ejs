<%_ /*
  template data object:
  {
    type: string | string[];
    name: string;
    subTypes: string[];
    example: string;
    link: string;
    desc: string;
    children?: TypeDocData[];
    isRequired?: boolean;
    level: number;
  }
*/ _%> 
<%- include('partials/complete-desc', {name, subTypes, example, children, level:1, isRequired, link, defaultValue, fragment}) -%> 
import chokidar from 'chokidar';
import { State } from '../lib/DataServer';
import { Config } from '../config/argv';
export declare const WATCH_IGNORE: RegExp;
export declare const createWatcher: (glob: any, config: Config) => chokidar.FSWatcher;
export declare const state: (config: Config, dev?: boolean | undefined) => State;

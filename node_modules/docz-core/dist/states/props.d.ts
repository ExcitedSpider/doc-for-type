import { State, Params } from '../lib/DataServer';
import { Config } from '../config/argv';
export declare const getPattern: (config: Config) => string[];
export declare const initial: (config: Config, pattern: string[]) => (p: Params) => Promise<void>;
export declare const state: (config: Config, dev?: boolean | undefined) => State;
